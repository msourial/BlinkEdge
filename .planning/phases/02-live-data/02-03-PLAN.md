---
phase: 02-live-data
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/components/MatchSelector.tsx
  - app/components/MatchCard.tsx
  - app/components/ApiStatusIndicator.tsx
  - app/components/DataErrorBanner.tsx
  - vitest.config.ts
  - app/page.tsx
autonomous: true
requirements:
  - LIVE-02
user_setup: []
must_haves:
  truths:
    - "Match selection screen displays 3 seeded fixtures with team names, scores, time/status"
    - "Tapping a match card dispatches SELECT_MATCH, transitions to camera init"
    - "Fade transition (~300ms) animates between match list and camera+HUD view"
    - "API status indicator shows connection state (cyan dot=connected, amber pulse=reconnecting, magenta=disconnected) in top-right area"
    - "Data error banner shows 'Connection Lost' with retry button when in API_ERROR state"
    - "Stale data banner shows 'Stale Data' with last-updated timestamp when connection is stale"
    - "User can return to match selection from camera HUD via GO_BACK"
    - "Connection status context + app state dispatch are wired in page.tsx"
  artifacts:
    - app/components/MatchSelector.tsx
    - app/components/MatchCard.tsx
    - app/components/ApiStatusIndicator.tsx
    - app/components/DataErrorBanner.tsx
    - app/page.tsx (modified)
    - vitest.config.ts (modified)
  key_links:
    - "MatchSelector → FIXTURE_IDS config → renders match cards"
    - "MatchSelector → dispatch(SELECT_MATCH) → transitions app state"
    - "ApiStatusIndicator → useConnectionStatus() → renders colored dot"
    - "DataErrorBanner → appState + useConnectionStatus → conditionally renders"
    - "page.tsx → appStateReducer → renders MatchSelector or camera+HUD via fade transition"
---

<objective>
Create all UI components for Phase 2: match selection screen, API status indicator, error/stale data banner. Integrate the app state machine and connection status into page.tsx with fade transitions.

Purpose: Complete the user-facing live data experience — match selection pre-camera, connection awareness during live view, graceful error handling, and smooth transitions between screens.

Output: 4 new app/components/*.tsx files, 1 modified page.tsx, 1 modified vitest.config.ts.
</objective>

<execution_context>
@/Users/m/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/m/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-live-data/02-RESEARCH.md
@.planning/phases/02-live-data/02-PATTERNS.md
@.planning/phases/02-live-data/02-UI-SPEC.md
@.planning/phases/02-live-data/02-CONTEXT.md
@app/page.tsx
@app/components/Scoreboard.tsx
@app/components/OddsMatrix.tsx
@app/components/CameraBackdrop.tsx
@lib/txline/txLineFixtureIds.ts
@lib/txline/txLineAppMachine.ts
@lib/txline/useConnectionStatus.ts
@lib/txline/TxLineProvider.tsx
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MatchSelector + MatchCard Components</name>
  <files>
    app/components/MatchSelector.tsx (NEW)
    app/components/MatchCard.tsx (NEW)
    vitest.config.ts (MODIFIED)
  </files>
  <action>
    Create the match selection UI components and update vitest config:

    1. **`app/components/MatchCard.tsx`** (per UI-SPEC Sec "Component Inventory", PATTERNS.md Sec "MatchCard.tsx", OddsMatrix.tsx card pattern):
    - `"use client"` directive
    - Import type from `@/lib/txline/txLineFixtureIds` (the `typeof FIXTURE_IDS[number]` shape)
    - `MatchCard({ fixture, onSelect }: { fixture: {...}, onSelect: () => void })`:
      - Card container: `bg-black/40 backdrop-blur-xl border border-white/10 rounded-2xl p-4` (same as Scoreboard.tsx/OddsMatrix.tsx card style)
      - Inside: row layout with 3 columns (home team / score+time / away team)
      - Home team name: 14px weight 600 (semibold), `font-sans`, left-aligned
      - Away team name: 14px weight 600 (semibold), `font-sans`, right-aligned
      - Score display: 24px weight 600 (semibold), `font-mono text-slate-200`, centered — use `0 - 0` placeholder for pre-match
      - Time/status: 12px weight 400, `text-xs text-slate-500 font-mono`, centered below score — show "LIVE" if kickoff has passed, else show formatted kickoff time
      - Competition label: 10px weight 400, `text-[10px] text-slate-600 font-mono`, centered below teams
      - Click handler: `onClick={onSelect}`
      - Hover effect: `hover:bg-white/10 transition-all duration-200`
      - Active/selected highlight (optional): cyan border `border-cyan-400/50` when selected
      - Follow existing interaction patterns from OddsMatrix.tsx: `cursor-pointer`, `transition-all duration-200`, no `rounded-full` (stays `rounded-2xl` per DSGN-07)

    2. **`app/components/MatchSelector.tsx`** (per D-09, D-11, UI-SPEC Sec "Component Inventory" + "State: Match Selection", PATTERNS.md Sec "MatchSelector.tsx"):
    - `"use client"` directive
    - Import `FIXTURE_IDS` from `@/lib/txline/txLineFixtureIds`
    - Import `MatchCard` from `./MatchCard`
    - `MatchSelector({ onSelectMatch, appState }: { onSelectMatch: (matchId: number) => void; appState: AppState })`:
      - Full-height scrollable container: `h-full overflow-y-auto` with `contain: layout paint` for GPU budget
      - Gradient backdrop: same radial-gradient as existing app (matching CameraBackdrop.tsx gradientBackdrop style)
      - **Header**: "Live Matches" in `text-xl font-bold font-mono` (uppercase tracking-wider, cyan accent)
      - **Loading state** (`appState.phase === "LOADING"`):
        - 3 skeleton cards using `animate-pulse` on card placeholders (bg-white/5 rounded-2xl p-4 h-24)
        - Each skeleton card: greyed out shimmer rectangles for team names + score
      - **Loaded state** (`appState.phase === "MATCH_SELECT"`):
        - Iterate `FIXTURE_IDS.map(fixture => <MatchCard key={fixture.fixtureId} ... />)`
        - Each MatchCard receives `onSelect` that calls `onSelectMatch(fixture.fixtureId)`
      - **Empty state** (when FIXTURE_IDS is empty — guard against runtime edge case):
        - Centered card: "No Live Matches" heading (20px mono weight 600)
        - Body: "Check back when World Cup fixtures are live, or pull to refresh." (14px weight 400)
        - Content matches UI-SPEC copywriting contract exactly
      - **Error state** (when appState.phase === "API_ERROR"):
        - Centered card with amber border/accent
        - Heading: "Connection Lost" (20px mono weight 600)
        - Body: "Can't reach TxODDS data. Check your connection and try again." (14px weight 400)
        - "Retry" button (amber-accent, matching CameraBackdrop.tsx buttonBase style)
      - Scroll container has `padding-bottom: env(safe-area-inset-bottom)` for iPhone home indicator

    3. **`vitest.config.ts`** (MODIFIED):
    - Update `test.include` array: add `"app/**/*.test.tsx"` to the existing patterns
    - Change test environment to support React: the safest approach is adding `// @vitest-environment jsdom` at the top of each component test file rather than changing the global environment (which would break existing node-based tests). Add a comment to vitest.config.ts noting this.
    - Example comment: `// See individual test files for @vitest-environment jsdom directive`
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict app/components/MatchCard.tsx app/components/MatchSelector.tsx 2>&1 || true</automated>
  </verify>
  <done>
    MatchCard renders fixture with team names, scores, time/status per UI-SPEC typography contract. MatchSelector renders scrollable fixture list with loading/loaded/empty/error states. vitest.config.ts updated with app/ test include pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: ApiStatusIndicator + DataErrorBanner</name>
  <files>
    app/components/ApiStatusIndicator.tsx (NEW)
    app/components/DataErrorBanner.tsx (NEW)
  </files>
  <action>
    Create connection status and error banner components:

    1. **`app/components/ApiStatusIndicator.tsx`** (per UI-SPEC Sec "State: API Connection" + "Component Inventory", PATTERNS.md Sec "ApiStatusIndicator.tsx"):
    - `"use client"` directive
    - Import `useConnectionStatus` from `@/lib/txline/useConnectionStatus`
    - `ApiStatusIndicator()`:
      - `const status = useConnectionStatus()`
      - Pill container: small, fixed or inline in HUD top-right (`absolute top-2 right-2 sm:top-4 sm:right-4 z-30`)
      - Render 8px indicator dot + label:
        - Dot: `w-2 h-2 rounded-full` with color mapping per UI-SPEC:
          - `connecting`: `bg-amber-400` with `animate-pulse` and `shadow-[0_0_8px_rgba(255,184,0,0.5)]`
          - `connected`: `bg-cyan-400` with `shadow-[0_0_8px_rgba(0,240,255,0.5)]`
          - `reconnecting`: `bg-amber-400` with `animate-pulse` and `shadow-[0_0_12px_rgba(255,184,0,0.5)]`
          - `disconnected`: magenta equivalent (`shadow-[0_0_8px_rgba(255,0,229,0.5)]`) — use inline style `boxShadow` since magenta isn't a Tailwind named color; `style={{ boxShadow: "0 0 8px rgba(255,0,229,0.5)" }}`
          - `stale`: `bg-amber-400` with slower pulse — use custom CSS or `animate-pulse` (same visual)
        - Label text per UI-SPEC copywriting:
          - connected → "Connected" (text-[10px] uppercase tracking-wider mono, text-cyan-300)
          - connecting → "Connecting…" (text-[10px] mono, text-amber-300)
          - reconnecting → "Reconnecting…" (text-[10px] mono, text-amber-300)
          - disconnected → "Disconnected" (text-[10px] mono, text-magenta-300 — use inline `color: "#ff00e5"` or closest Tailwind)
          - stale → "Stale Data" (text-[10px] mono, text-amber-300)
      - Pill wrapper: `bg-black/60 backdrop-blur-md border border-white/10 rounded-full px-2 py-1 flex items-center gap-1.5`
      - Follow existing minimal presentational component pattern from Scoreboard.tsx

    2. **`app/components/DataErrorBanner.tsx`** (per UI-SPEC Sec "Component Inventory" + "Copywriting Contract", PATTERNS.md Sec "DataErrorBanner.tsx"):
    - `"use client"` directive
    - Import `useConnectionStatus` from `@/lib/txline/useConnectionStatus`
    - Import types for dispatch: accept `onRetry: () => void` and `onGoBack: () => void` as props
    - `DataErrorBanner({ appState, onRetry, onGoBack }: { appState: AppState; onRetry: () => void; onGoBack: () => void })`:
      - Banner at bottom: `fixed bottom-0 inset-x-0 z-40 p-4` with padding for safe-area
      - Background: `bg-amber-500/10 border-t border-amber-500/20 backdrop-blur-xl`
      - **API_ERROR state**:
        - Heading "Connection Lost" (text-xl weight 600 mono)
        - Body "Can't reach TxODDS data. Check your connection and try again." (text-sm weight 400, text-slate-300)
        - "Retry" button: amber accent, border-amber-400/50, bg-amber-500/10 hover:bg-amber-500/20, rounded-lg, px-4 py-2, text-sm, uppercase tracking-wider, `onClick={onRetry}`
      - **Stale state** (connection status === "stale" AND appState.phase === "AR_HUD_LIVE"):
        - Heading "Stale Data" (text-xl weight 600 mono)
        - Body "Last data received {time} ago. Results may be outdated." with relative time (use a simple elapsed-message or just "over 30s ago")
        - "Go Back" button (less prominent, text-slate-400): `onClick={onGoBack}`
      - **Offline state** (appState.phase === "OFFLINE"):
        - Render nothing — offline shell handles its own UI
      - No overlay when connection is "connected" — return null
      - Transition in/out: `transition-all duration-300` with conditional opacity/translate-y

    **Style guidance:** Use Tailwind classes and inline-style color overrides for non-standard colors (magenta #ff00e5, amber #ffb800) — follow the pattern from CameraBackdrop.tsx which uses inline `style={{ color: "var(--color-amber)" }}` for custom colors.
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict app/components/ApiStatusIndicator.tsx app/components/DataErrorBanner.tsx 2>&1 || true</automated>
  </verify>
  <done>
    ApiStatusIndicator renders colored dot + text label for all 5 connection states per UI-SPEC visual contract. DataErrorBanner shows connection-lost/stale banners with retry/go-back buttons per UI-SPEC copywriting.
  </done>
</task>

<task type="auto">
  <name>Task 3: page.tsx App State Integration + Fade Transitions</name>
  <files>
    app/page.tsx (MODIFIED)
  </files>
  <action>
    Integrate the app state machine, connection status, new components into page.tsx:

    1. **Replace imports** (current page.tsx lines 1-10):
    - Keep existing imports: `React`, `useState`, `useRef`, `WalletProvider`, `TxLineProvider`, `RiskEngineProvider`, `Scoreboard`, `ConsensusIndicator`, `OddsMatrix`, `MockHedgeModal`
    - Add: `import { useReducer } from "react"`
    - Add: `import { appStateReducer, INITIAL_APP_STATE, AppAction } from "@/lib/txline/txLineAppMachine"`
    - Add: `import { ConnectionStatusProvider } from "@/lib/txline/useConnectionStatus"`
    - Add: `import { useConnectionBus } from "@/lib/txline/TxLineProvider"` — only if `useConnectionBus` is used directly; otherwise use `ConnectionStatusProvider` wrapper
    - Add: `import { MatchSelector } from "./components/MatchSelector"`
    - Add: `import { ApiStatusIndicator } from "./components/ApiStatusIndicator"`
    - Add: `import { DataErrorBanner } from "./components/DataErrorBanner"`

    2. **Replace state management** (replace `isCameraActive` useState + `activeBet`):
    - Keep `activeBet` state (used by MockHedgeModal)
    - Replace `isCameraActive` with: `const [appState, dispatch] = useReducer(appStateReducer, INITIAL_APP_STATE)`
    - On mount: dispatch `{ type: "LOADED" }` to transition LOADING → MATCH_SELECT (after a brief timeout or immediately)

    3. **Add MatchSelector provider wrapper**:
    - The MatchSelector and ApiStatusIndicator need access to ConnectionStatusProvider and the appState dispatch
    - Create an `AppShell` inner component (or use inline) that receives `appState` and `dispatch` and renders the conditional views
    - Structure:
      ```
      <TxLineProvider>
        <ConnectionStatusProvider connectionBus={...}>
          <RiskEngineProvider>
            <WalletProvider>
              <AppContent appState={appState} dispatch={dispatch} />
            </WalletProvider>
          </RiskEngineProvider>
        </ConnectionStatusProvider>
      </TxLineProvider>
      ```
    - **Issue:** `ConnectionStatusProvider` needs the `connectionBus` from `TxLineProvider`. Since both are inside each other, use a nested approach: the inner `AppContent` component uses `useConnectionBus()` to get the bus, then creates a `ConnectionStatusProvider` wrapping just the UI.
    - Alternately, restructure so `TxLineProvider` exposes connectionBus in its context (which it does per Plan 02-01) and then an inner wrapper extracts it and renders `ConnectionStatusProvider`. See PATTERNS.md for provider composition.

    4. **Conditional rendering with fade transitions** (per D-09, D-10, RESEARCH.md Sec "Match Selection Fade Transition Pattern"):
    ```tsx
    {/* Match Selection Screen — fade in/out */}
    <div className={`transition-opacity duration-300 ${
      appState.phase === "MATCH_SELECT" || appState.phase === "LOADING" || appState.phase === "API_ERROR"
        ? "opacity-100" 
        : "opacity-0 pointer-events-none"
    }`}>
      <MatchSelector onSelectMatch={(id) => dispatch({ type: "SELECT_MATCH", matchId: id })} />
    </div>

    {/* Camera + HUD Screen — fade in/out */}
    <div className={`transition-opacity duration-300 ${
      appState.phase === "CAMERA_INIT" || appState.phase === "AR_HUD_LIVE"
        ? "opacity-100" 
        : "opacity-0 pointer-events-none"
    }`}>
      {/* existing camera + HUD layout */}
    </div>
    ```

    5. **Camera flow adaptation**:
    - The existing `startARCamera` function (`onClick={startARCamera}` on the "Tap to Scan" button) is replaced: camera starts automatically when `CAMERA_INIT` is dispatched by MatchSelector
    - Modify the camera init flow: when `appState.phase === "CAMERA_INIT"`, useEffect triggers camera startup
    - On camera success: `dispatch({ type: "CAMERA_READY" })`
    - On camera failure: `dispatch({ type: "API_ERROR", error: "Camera unavailable" })`
    - Remove the old `!isCameraActive` guard; replace with the appState-based conditional rendering

    6. **Add API status indicator**:
    - `<ApiStatusIndicator />` appears in the top-right of the camera+HUD view (inside the camera HUD div, absolute positioned)
    - Only visible during `AR_HUD_LIVE` phase

    7. **Add DataErrorBanner**:
    - `<DataErrorBanner appState={appState} onRetry={() => dispatch({ type: "RETRY" })} onGoBack={() => dispatch({ type: "GO_BACK" })} />` 
    - Rendered inside the outer div (persistent across screens) — it will conditionally render based on appState

    8. **Keep existing HUD layout unchanged** (lines 68-81 of current page.tsx):
    - Scoreboard, OddsMatrix, ConsensusIndicator positions remain identical
    - MockHedgeModal remains identical
    - Camera video element stays
    - The phone frame container structure is unchanged
    - Remove or replace only the `{!isCameraActive && (...)}` section with the fade-wrapped sections

    9. **Add GO_BACK action**:
    - In the camera+HUD view, provide a back button (or wire to existing gesture) that dispatches `{ type: "GO_BACK" }` to return to match selection
    - Small left-arrow button in the top-left of the HUD view: subtle, `text-xs text-slate-500 hover:text-slate-300`, positioned absolute top-4 left-4

    **IMPORTANT:** Do NOT restructure the layout or change visual styling of existing HUD widgets. The only visual changes are:
    - MatchSelector replaces the "Tap to Scan" pre-camera screen
    - ApiStatusIndicator is a small new element in top-right corner
    - DataErrorBanner appears conditionally at bottom
    - Fade transitions between match list and camera view
    - Back button to return to match selection
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict app/page.tsx 2>&1 || true</automated>
  </verify>
  <done>
    page.tsx uses appState reducer instead of isCameraActive boolean. MatchSelector renders during MATCH_SELECT phase. Camera+HUD renders during AR_HUD_LIVE phase. ApiStatusIndicator visible in camera view. DataErrorBanner shown on error/stale. Fade transition (300ms) between screens. Back button in HUD dispatches GO_BACK. All existing HUD widgets and layout preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MatchSelector → AppState dispatch | User tap transitions app state. No external input beyond fixture ID (from hardcoded config). |
| ApiStatusIndicator → ConnectionStatusContext | Read-only display component; no mutation of connection state. |
| DataErrorBanner → dispatch(RETRY) | User tap triggers retry action. No side effects beyond state transition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Information Disclosure | page.tsx bundle | low | accept | No secrets or tokens in page.tsx — all auth flows in lib/txline layer. |
| T-02-09 | Elevation of Privilege | dispatch from components | low | accept | Components only dispatch typed AppActions defined in appStateReducer. No arbitrary dispatch. |
</threat_model>

<verification>
- [ ] `npx tsc --noEmit` passes for all new and modified files
- [ ] `npx next build` succeeds with new components
- [ ] Manual check: MatchSelector renders 3 fixtures at `/`
- [ ] Manual check: Tapping a match triggers camera flow
- [ ] Manual check: Fade transition animates between match list and camera view
- [ ] Manual check: Back button in HUD returns to match selection
- [ ] Manual check: ApiStatusIndicator shows "Connecting…" → "Connected" (devnet)
- [ ] Manual check: DataErrorBanner shows when connection lost (airplane mode)
</verification>

<success_criteria>
1. MatchSelector renders 3 seeded fixtures with team names and tap-to-select
2. MatchCard renders per UI-SPEC typography contract (14px semibold team names, 24px mono scores, 12px mono time)
3. Fade transitions (300ms) animate between match list and camera+HUD views
4. ApiStatusIndicator shows connection dot + label for all 5 states
5. DataErrorBanner shows "Connection Lost" with retry on API_ERROR, "Stale Data" on stale
6. Back button in HUD view dispatches GO_BACK to return to match selection
7. All existing HUD widgets (Scoreboard, OddsMatrix, ConsensusIndicator) render identically
8. MockHedgeModal continues to work unchanged
9. TypeScript compiles without errors
</success_criteria>

<output>
Create `.planning/phases/02-live-data/02-03-SUMMARY.md` when done
</output>
