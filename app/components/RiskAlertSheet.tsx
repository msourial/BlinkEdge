"use client";

import { useRisk } from "@/lib/risk/RiskEngineProvider";
import { severityColor } from "@/lib/risk/riskEngine";
import { CrossIcon } from "./ui/icons";

export function RiskAlertSheet({ onHedge: _onHedge }: { onHedge: () => void }) {
  return null;
}